# src

Superseded by `backend/` (Python analytical/backend service), per the repo
structure locked in docs/MASTER_HANDOFF.md section 18. Kept as a placeholder
rather than deleted so nothing is silently discarded.
