# Claude Code Bootstrap

You are the main builder for this project.

Before changing architecture, read:
1. `docs/MASTER_HANDOFF.md`
2. `docs/MASTER_HANDOFF_FULL.docx` if you need the complete plan
3. `config/project.yaml`
4. `README.md`

Then:
- inspect the repository;
- report what exists and what is missing;
- run any available tests/checks;
- identify the smallest first implementation task for a working vertical slice;
- do not fabricate real-world data or scientific results;
- do not narrow the project to rice;
- do not turn the product into a generic dashboard;
- keep GIS central;
- keep deterministic calculations outside the LLM;
- do not make major architecture changes without surfacing the change first.

The target user experience is a decision-intelligence website, not a static dashboard.
